为你量身定制的这两个项目的 PRD（产品需求文档）大纲。作为用于“包装简历”和“应对面试”的 PRD，重点不仅在于业务逻辑，更在于**凸显技术栈（Next.js/LangGraph/NestJS/Docker等）在业务场景中的合理性与不可替代性**。

你可以将这些内容提炼后写入简历的项目描述中，或者在面试被问到“项目的整体架构和业务背景”时，直接抛出这些专业的产品设计思路。

# 📄 项目一 PRD：企业级多智能体数据洞察平台 (AI-BI Multi-Agent Platform)

## 1. 产品概述与背景

- **产品名称**：DataMind AI-BI 平台
- **业务痛点**：传统 BI 平台重度依赖数据分析师编写 SQL 和配置仪表盘。业务侧（运营、产品）提数需求多、排期长，导致数据洞察滞后。同时，市面上单节点的 LLM 极易在复杂的数据库结构下产生“幻觉”，生成错误 SQL。
- **产品愿景**：打造一个“对话即图表”的智能数据终端，通过 LangGraph 引入多智能体（Multi-Agent）协作与自纠错机制，让非技术人员能 100% 准确地通过自然语言获取数据报表。

## 2. 目标用户

- **业务人员（核心受众）**：产品经理、市场运营（通过自然语言提问，获取数据和图表）。
- **数据管理员（配置者）**：DBA 或数据分析师（负责接入数据源，配置表结构元数据和数据字典）。

## 3. 核心功能模块与用户故事 (User Stories)

### 3.1 智能对话与图表渲染模块 (Frontend - Next.js)

- **需求描述**：提供类似 ChatGPT 的对话流界面。不仅能输出文本，还需解析后端返回的特定 JSON 结构，动态渲染 Echarts/Recharts 图表。
- **用户故事**：作为运营，我输入“对比过去三个月华东区和华南区的销售额趋势”，系统能流式打字输出分析结论，并配上一张动态折线图，我可以将该图表一键导出或加入我的个人 Dashboard。

### 3.2 多智能体工作流模块 (AI Core - LangGraph & LangChain)

- **需求描述**：后端的 AI 大脑，由多个 Agent 组成状态机。
- **Agent 分工**：
  - **Planner Agent**：理解用户意图，拆解查询步骤，识别需要查询的表。
  - **SQL Agent**：根据表结构（Schema）生成 SQL。
  - **Review Agent (纠错节点)**：如果 SQL 执行报错，Review Agent 会捕捉报错信息并指导 SQL Agent 重新生成（LangGraph 的循环纠错能力）。
  - **Analyst Agent**：对查询出的 JSON 数据进行总结，生成业务洞察。
  - **Chart Agent**：生成匹配前端的图表配置代码（JSON）。

### 3.3 数据安全与沙盒执行环境 (Security & Infra - Docker & NestJS)

- **需求描述**：AI 生成的 SQL 可能存在破坏性（如 `DROP TABLE`）或导致慢查询打垮线上库。
- **用户故事**：作为系统架构师，我需要确保 AI 只能在一个严格隔离的沙盒中执行查询，且有超时打断机制。
- **技术实现**：NestJS 接收执行指令，通过 Docker API 动态拉起一个隔离的容器（或直连只读的从库），在沙盒环境中执行 SQL，确保生产环境绝对安全。

### 3.4 会话记忆与持久化 (Storage - PostgreSQL + Prisma)

- **需求描述**：支持多轮对话上下文。
- **技术实现**：利用 PostgreSQL 存储多轮对话历史，Prisma 管理数据模型。结合 LangGraph 的 `Checkpointer` 实现图状态的持久化，用户刷新页面后对话不丢失